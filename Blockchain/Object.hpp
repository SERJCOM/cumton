#pragma once

#include <vector>
#include <algorithm>
#include <ostream>

// namespace cumton::blockchain{
//     struct Object{

//         virtual void PrintObjectData(std::ostream& stream) = 0;

//         static void WriteDataToStream(std::ostream& stream, char* data, size_t len){
//             stream.write(data, len);
//         }

//     };
// }